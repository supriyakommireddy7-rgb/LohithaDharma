# The URL of the backend API
# In production, this can be set to your backend URL (e.g. https://api.yourdomain.com)
# In development, leave this empty to use Vite's built-in proxy (which routes /api to the backend)
VITE_API_URL=
